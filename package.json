{
  "name": "sov-test-prisma-postgres",
  "version": "1.0.0",
  "description": "Express + Prisma + Postgres - Sovereign import certification",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "db:migrate": "npx prisma migrate deploy",
    "db:seed": "node prisma/seed.js",
    "postinstall": "npx prisma generate"
  },
  "dependencies": {
    "express": "^4.18.2",
    "@prisma/client": "^5.9.0"
  },
  "devDependencies": {
    "prisma": "^5.9.0"
  }
}
